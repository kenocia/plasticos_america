<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="group_sales_commission_user" model="res.groups">
        <field name="name">Comisiones de ventas / Usuario</field>
        <field name="implied_ids" eval="[(4, ref('base.group_user')), (4, ref('account.group_account_invoice')), (4, ref('hr.group_hr_user'))]"/>
    </record>

    <record id="group_sales_commission_manager" model="res.groups">
        <field name="name">Comisiones de ventas / Responsable</field>
        <field name="implied_ids" eval="[(4, ref('kc_sales_commission.group_sales_commission_user'))]"/>
    </record>

    <!-- ir.rule: creadas en hooks.py (SQL) para no pasar por _check_domain del core / ir.model roto -->
</odoo>
